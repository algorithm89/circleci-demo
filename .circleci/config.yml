version: 2.1
jobs:
  build:
    docker:
      - image: alpine:latest
    steps:
      - run:
          name: The First Step
          command: |
            apk update && apk add add net-tools
            ping 4 -c google.com
            apk update -y && apk add bind-tools 
            nslookup gamespot.com
            dig gamespot.com