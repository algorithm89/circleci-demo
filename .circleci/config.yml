version: 2.1
jobs:
  build:
    docker:
      - image: alpine:latest
    steps:
      - run:
          name: The First Step
          command: |
            apk update && apk add bind-tools          
            apk update && apk add net-tools
            nslookup gamespot.com
            ping 4 -c google.com
            dig gamespot.com